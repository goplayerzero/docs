export const LandingCard = ({ 
  backgroundColor = "#6753FF", 
  boldText, 
  followUpText,
  icon,
  href = "#"
}) => {
  return (
    <a href={href} className="block border border-white rounded-lg no-underline w-full h-full">
      <div className="rounded-lg p-6 w-full h-full transition-all duration-200 hover:scale-105" style={{ backgroundColor }}>
        <div className="flex flex-col h-full">
          <div className="mb-8">
            <div className="text-base text-white">
              <span className="font-bold text-lg block mb-1">{boldText}</span>
              <span className="font-normal opacity-90">{followUpText}</span>
            </div>
          </div>
          
          <div className="mt-auto">
            {icon === 'lightning' && (
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M13 10V3L4 14H11V21L20 10H13Z" stroke="white" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"/>
              </svg>
            )}
            
            {icon === 'square' && (
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                <rect x="3" y="3" width="18" height="18" rx="2" stroke="white" strokeWidth="2"/>
              </svg>
            )}
          </div>
        </div>
      </div>
    </a>
  );
};
